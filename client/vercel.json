{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://$VITE_API_HOST/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
