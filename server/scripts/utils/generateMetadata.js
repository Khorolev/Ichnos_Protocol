/**
 * Re-export metadata generation utilities from helpers.
 *
 * Provides a convenient import path for extraction scripts:
 *   import { generateMetadata, VALID_CATEGORIES } from './utils/generateMetadata.js';
 *
 * @module utils/generateMetadata
 */
export { generateMetadata, VALID_CATEGORIES } from "../helpers/metadataGenerator.js";
