/**
 * Re-export heading-based Markdown chunking from helpers.
 *
 * Provides a convenient import path for extraction scripts:
 *   import { chunkMarkdownByHeadings } from './utils/chunkByHeadings.js';
 *
 * @module utils/chunkByHeadings
 */
export { chunkMarkdownByHeadings } from "../helpers/markdownChunker.js";
